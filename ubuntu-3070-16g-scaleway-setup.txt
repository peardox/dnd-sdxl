create RENDER-S GPU instance with 125G disk space (min allowed) then log into it and...
# Cost 1.2426 eu / hour (final charge = 1.25 eu for about an hour)

# note the 3070 will fail OOM if cuda used so must need 10G+?

# Create 8G swap
dd if=/dev/zero of=/swapfile1 bs=1024 count=8388608
chown root:root /swapfile1
chmod 0600 /swapfile1
mkswap /swapfile1
swapon /swapfile1

add to /etc/fstab
/swapfile1 none swap sw 0 0

# possibly create a user?


add to .bash_rc
export PATH=$HOME/.local/bin:$PATH

source .bash_rc
sudo apt-get update
sudo apt-get upgrade
sudo apt-get install python3-pip python-is-python3 nvidia-utils-535 nvidia-driver-535
sudo reboot

nvidia-smi
git clone https://github.com/peardox/dnd-sdxl.git
cd dnd-sdxl
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
pip install torch onnx transformers diffusers accelerate
[leave use_cuda = False (not enough GPU memory)]
python dnd.py

dnd.py Runtime = 0 hours 27 mins 45 secs

# ls -la --time-style=+"%Y-%m-%d %H:%M:%S" dnd/* | wc
