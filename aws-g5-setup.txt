create AWS G5 instance with a minumum 30G disk space then log into it and...

add to .bash_rc
export PATH=$HOME/.local/bin:$PATH

source .bash_rc
sudo apt-get update
sudo apt-get upgrade
sudo apt-get install python3-pip python-is-python3 nvidia-utils-535 nvidia-driver-535
sudo reboot

nvidia-smi
git clone https://github.com/peardox/dnd-sdxl.git
cd dnd-sdxl
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
pip install torch onnx transformers diffusers accelerate
enable cuda in manyjobs.py (use_cuda = True)
python manyjobs.py

Runtime = 0 hours 12 mins 50 secs
